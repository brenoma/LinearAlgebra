import static MenuOptions.MenuCLI.menuOptions;

public class Main {

    public static void main(String[] args) {
        menuOptions();

        /**
         * Matrices Operations
         */

//        MatrixImpl m1 = new MatrixImpl(3, 2);
//        MatrixImpl m2 = new MatrixImpl(3, 4);

//        service.dot(m1.getMatrix(), m2.getMatrix());
//        service.sum(m1.getMatrix(), m2.getMatrix());

//        MatrixImpl m3 = new MatrixImpl(3, 2);
//        service.transpose(m3.getMatrix());
//        service.times(2, m3.getMatrix());

        /**
         * Vector Operations
         */

//        VectorImpl v1 = new VectorImpl(2);
//        VectorImpl v2 = new VectorImpl(2);

//        service.sum(v1.getVector(), v2.getVector());
//        service.times(2, v1.getVector());
//        service.dot(m1.getMatrix(), v1.getVector());
    }
}
